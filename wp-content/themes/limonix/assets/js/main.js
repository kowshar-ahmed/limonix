/**
 * Limonix — Main theme JavaScript.
 *
 * Kept intentionally minimal at this stage. Add interactive behaviour
 * (mobile menu toggle, etc.) here as the theme grows.
 *
 * @package Limonix
 */

( function () {
	'use strict';

	document.addEventListener( 'DOMContentLoaded', function () {
		// Placeholder: theme scripts will be initialized here.
	} );
} )();
